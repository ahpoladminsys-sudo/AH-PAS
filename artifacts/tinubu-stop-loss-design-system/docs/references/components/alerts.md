# Alerts and notices pattern

**Source evidence:** `artifacts/stop-loss-app/index.html`, `.sl-alert`,
`.sl-rfp-status`, and sync/status notices.

This is a pending shared family. Alerts should distinguish information,
successful completion, review-needed warnings, and blocking errors using both
semantic color and plain-language status text.