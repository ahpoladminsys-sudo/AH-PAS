# Navigation and tabs pattern

**Source evidence:** `artifacts/stop-loss-app/index.html`, `.tab`, `.detail-tabs`,
`.nav`, and workspace shell navigation.

This is a pending shared family. The source repeats tab and detail navigation,
but its hierarchy and state are tightly coupled to product-specific views.
